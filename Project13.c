#include <stdio.h>

int main() {
	
                int var;
                printf("veuillez entrer la valeur");
                scanf("%d",&var);
                printf("la valeur en octal est: %o",var);
                printf("la valeur en hexadicimal est: %x",var );
	return 0;
}
